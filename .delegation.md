Owner meminta eksekusi refactoring penuh. Semua audit sudah selesai.

PRINSIP:
- Best practice, clean code, scalable, maintainable, user friendly, zero bug/error.
- Lokasi repo: D:/CRM-AI/ (MSYS: /d/CRM-AI).
- Kerjakan di folder lokal /d/CRM-AI, jangan clone ulang.
- Jangan hapus file legacy yang masih dipakai production. Refactoring hanya di v2-core/ folder dan file security/devops tertentu.
- Dokumentasi setiap perubahan di file .execution-log.md di root.

---

T1+T2: [@02-Fullstack-Builder] — Perbaikan Backend v2-core + Frontend v2-core

Baca audit: /d/CRM-AI/audit-v2-core.md dan /d/CRM-AI/audit-legacy.md

Backend (prioritas):
1. Login.tsx (BLOCKER): hooks define [username, setUsername] dan [password, setPassword], tapi input pakai value={user}/{pass} dan setUser/setPass. Ganti ke variable benar. Tambah import KeyRound dari lucide-react.
2. settings.controller.ts: whatsappService.getStoreStatus() tidak ada. Ganti fallback sederhana.
3. stores.controller.ts: destroyClient() -> logoutClient().
4. PausedContact model: tambah field paused_until (DATE, nullable) agar chat.controller.ts upsert berfungsi.
5. followup.controller.ts line 51: ganti response:'...' -> cancel_reason:'Emergency cancel by system'.
6. media.controller.ts: hapus panggilan ms.processNewMedia(), cukup return asset.
7. Socket.IO: app.ts tambah event emitter qr, ready, disconnected, newMessage, dashboardUpdate dari WA engine.
8. Pastikan auth router accept { user, pass } dari frontend.

Frontend:
1. ChatManagement.tsx: event newMessage via Socket.IO.
2. Stores.tsx: event qr, ready, disconnected, render QR + status.
3. Dashboard.tsx: event dashboardUpdate.
4. Cek semua API path.

Quality: npx tsc --noEmit lolos di backend dan frontend. Tulis ringkasan di .execution-log.md.

---

T4: [@03-QA-Security-Reviewer] — Perbaikan Security

Baca audit: /d/CRM-AI/audit-security.md

Kerjakan LANGSUNG di file:
1. auth.controller.ts: hapus fallback hardcoded ADMIN_PASS='admin123'. Wajibkan env. Sinkron dengan env legacy.
2. public/index.html renderAgents(): escapeHtml() untuk innerHTML. Audit 30+ innerHTML lain.
3. public/index.html inline onclick: encodeURIComponent() atau dataset + addEventListener.
4. media.routes.ts: tambah multer fileFilter (gambar/video) + fileSize limit 50MB.
5. authStore.ts: pindah JWT dari localStorage ke sessionStorage.
6. auth.routes.ts: tambah express-rate-limit (10 req/15 menit).
7. Buat .env.example.

Tulis ringkasan di .execution-log.md.

---

T5: [@04-DevOps-Deployer] — Perbaikan DevOps

Baca audit: /d/CRM-AI/audit-devops.md

1. .gitignore: tambah cloudflared.exe, .cloudflared_backup/, *.env, .env.local, node_modules/, data/uploads/*, data/sessions/*, database.sqlite-shm, database.sqlite-wal, audit-*.md. Fix baris null bytes.
2. Dockerfile: multi-stage build, node:20-slim, gabung npm install, HEALTHCHECK, user non-root.
3. Port conflict: v2-core port dari 3001 -> 3002 (update env default dan frontend API base URL).
4. Buat .env.example (koordinasi dengan QA).
5. update.sh: fix PM2 name mismatch.
6. Backup script: fix path database.

Tulis ringkasan di .execution-log.md.

---

URUTAN:

Phase A (PARALEL):
- Builder: T1+T2 backend+frontend
- QA: T4 security
- DevOps: T5 devops

Phase B (serial, tunggu Phase A):
- Builder: integration test
- QA: verify

Phase C:
- Strategist: review final + laporan

MULAI.
